import 'package:flutter/material.dart';

class EngineeringExperience extends StatelessWidget {
  const EngineeringExperience({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Experience"));
  }
}
