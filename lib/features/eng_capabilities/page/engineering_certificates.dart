import 'package:flutter/material.dart';

class EngineeringCertificates extends StatelessWidget {
  const EngineeringCertificates({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Certificates"));
  }
}
